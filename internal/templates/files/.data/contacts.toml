# ============================================
# IDENTITIES (user-edited)
# Link multiple contacts to a single identity
# ============================================
[[identity]]
id = "{{username}}"
contacts = ["cli:{{username}}"]
allowed = true

# ============================================
# CONTACTS (auto-generated - do not edit)
# All contacts that have sent messages
# ============================================
